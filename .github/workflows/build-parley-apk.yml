name: Build Parley Live Smart APK

on:
  push:
    branches:
      - chatgpt-parley-smart
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out build branch
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Clone Parley
        run: git clone --depth 1 https://github.com/sync-kyoto-jp/parley.git parley

      - name: Apply GPT-Live smart interpreter changes
        run: |
          cat smart-overlay.part00 smart-overlay.part01 smart-overlay.part02 smart-overlay.part03 smart-overlay.part04 | base64 -d | tar -xz -C parley
          rm -f parley/app/src/main/java/jp/kyoto/sync/parley/net/TranslationSession.kt
          rm -f parley/app/src/main/java/jp/kyoto/sync/parley/net/RealtimeEvents.kt

      - name: Build debug APK
        working-directory: parley
        run: |
          chmod +x gradlew
          ./gradlew :app:assembleDebug --stacktrace

      - name: Collect APK
        run: |
          mkdir -p output
          cp parley/app/build/outputs/apk/debug/app-debug.apk output/Parley-Live-Smart.apk
          sha256sum output/Parley-Live-Smart.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: Parley-Live-Smart-apk
          path: output/Parley-Live-Smart.apk
          if-no-files-found: error
          retention-days: 7
